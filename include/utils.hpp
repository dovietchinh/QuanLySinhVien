#ifndef __UTILS__H__
#define __UTILS__H__
#include <string>
#include <vector>
using namespace std;
vector<string> split(string x,string sample);
string replace(string x, string temp1, string temp2);

#endif
